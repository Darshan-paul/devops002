git init
git add .
git status
git add . 
git status
git commit -m "Added index , logo ,style filr for website"

githun creating new repository
git remote add origin  <url>
git remote show origin 
git status

deployement - <build>
               <plugind>

git status
git add . 
git remote show origin
git commit -m "Added the docs folder for deployment"
git push -u origin main 
git status 

go back to git hub , goto settings >pages>main>doc'c>save>refresh


>src>test>java directory>new>package>org.test
>org.test>new>javaclass>WebPageTest and paste code

driver.get (url paste)
run

mvn clean package 
java -jar target/ click tab 
mvn clean 

mvn site

copy the <sidtribut>
after endof </build>
paste it 

mvn deploy
